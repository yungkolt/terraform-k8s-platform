apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes
  prune: false  # Don't prune infrastructure
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
